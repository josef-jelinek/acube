package acube;

public final class Options {
  public Metric metric; // metrics (lengths of moves) defined in Turn class
  public boolean findAll; // search for all shortest sequences
  public boolean findOptimal; // search only for optimal sequences (do not use phase B)
}
